{
  "description": "Data transformation pipes for console.log",
  "version": "1.0.6",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "name": "logpipes",
  "private": false,
  "scripts": {
    "build": "tsc",
    "clean": "rimraf ./dist",
    "lint": "eslint . --ext .ts",
    "test": "jest --runInBand",
    "release": "npm run clean && npm run build && npm run lint && npm run test && npm publish"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.61.0",
    "@typescript-eslint/parser": "^5.61.0",
    "eslint": "^8.44.0",
    "rimraf": "^5.0.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=10"
  },
  "files": [
    "dist/**/*"
  ],
  "keywords": [
    "logging",
    "console",
    "log",
    "json",
    "console.log",
    "console.debug"
  ],
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git@github.com:mfursov/logpipes.git"
  }
}
